# Note: this file is only being used for unit tests.
# The bootloader-tool generates the cert-block by creating a temporary file based on config.toml for normal operation.

family: mimxrt685s
revision: latest

rootCertificate0File: cert-rot1.pem
chainCertificate0File0: cert-img1.pem

rootCertificate1File: cert-rot2.pem
chainCertificate1File0: cert-img2.pem

# mainRootCertId: 0 # -- determined by application being signed.
containerOutputFile: cert-block.bin
